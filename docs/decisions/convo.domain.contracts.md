# `convo.domain.contracts`

The reasoning that used to live in the docstrings of `convo/domain/contracts.py`; the code keeps one line per symbol.

## SessionMeta

Unknown fields are ignored on purpose: an older worker must keep starting
against a newer control plane.
