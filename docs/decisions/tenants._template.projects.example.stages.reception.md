# `tenants._template.projects.example.stages.reception`

The reasoning that used to live in the docstrings of `tenants/_template/projects/example/stages/reception.py`; the code keeps one line per symbol.

## Reception.summary

Identity travels, state does not. A status read a minute ago is a
status that may have changed; the next stage asks the system instead of
inheriting a sentence. TODO(copy): keep this line to identity only.
