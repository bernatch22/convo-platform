# `convo.lang.es`

The reasoning that used to live in the docstrings of `convo/lang/es.py`; the code keeps one line per symbol.

## module

Pure functions that take the day as an argument, so every rule is a one-line
unit test. Today's date never reaches the system prompt; it arrives as a clock reading (`convo.agents.clock`).
